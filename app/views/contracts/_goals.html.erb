
<div class="center narrow table topmargin">
	<%= text_field_tag @level.name, nil,
	autofocus: true,
	placeholder: "Reduce #{@level.task.name} #{@level.name} to (#{@level.unit})",
	class: 'invis' %>	
</div>
